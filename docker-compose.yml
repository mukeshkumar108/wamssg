services:
  whatsapp-ingestor:
    build: .
    init: true
    restart: always
    stop_grace_period: 30s
    ports:
      - "3000:3000"
    volumes:
      - out-data:/app/out
      - auth-data:/app/.wwebjs_auth

volumes:
  out-data:
  auth-data:
